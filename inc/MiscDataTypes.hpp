/****************************************************************************/
/**
 ** @file   MiscDataTypes.hpp
 ** @brief  Define our basic data types.
 ** @author Johan Hendrik Ehlers <johanhendrikehlers@gmail.com>
 ** 
 **/

#ifndef __MISCDATATYPES_HPP__
#define __MISCDATATYPES_HPP__

/** 64-bit (or more) signed floating point number. */
typedef double SFloat64;

/** 32-bit signed floating point number. */
typedef float SFloat32;

/** 32-bit (or more) signed integer. */
typedef signed int SInt;

/** 32-bit (or more) unsigned integer. */
typedef unsigned int UInt;

/** 16-bit signed integer. */
typedef signed short SInt16;

/** 16-bit unsigned integer. */
typedef unsigned short UInt16;

/** 8-bit signed integer. */
typedef signed char SInt8;

/** 8-bit unsigned integer. */
typedef unsigned char UInt8;

/** A boolean. */
typedef bool Bool;

/** A character. */
typedef char Char;

#endif //__MISCDATATYPES_HPP__
